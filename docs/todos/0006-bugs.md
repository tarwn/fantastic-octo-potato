Almost Bugs:
1. When the runner performs an action, we need to include a text message back with the report of the step that is descriptive of what it is doing for better display in the Transcript. Add a body property named `target`, use the target or selector to identify what the action is acting on on the screen using Playwright to identify the component (if it's an input, simply report the type of input as `{type} input`, select => `Dropdown`, create a simple mapping to describe meaningful HTML inputs and then default the reset to the word "element") and then add a simple selector if possible (pick first scenario that is successfull: if it has an id, "id='{id}'", if is a button and has a label "label='{label}'", if it is a link use the inner text, if it is a text box look for a label linked by the text input's id, then look for closest label and see if it is a parent, then give up and leave it empty). Type this as `targetDescription: { component: string, selector: string }`. On the hub server we accept this value in `parseReportStepBody` as a required field, just like `stepId` and `outcome` and store it in the db the same way. When the transcript is loaded by the UI it shuld include this field in the data it gets from the API, then displays in the transcript row as `{stepId}: {this new field}` from the data instead of the description/message field with stepId and outcome baked in already, as this will be closer to the mockup. The JSON version of the transcript should have the stepId, outcome, and target as separate fields also, not a combined message field as it is now.

Bugs during testing:
1. When a job status is Intervention-Requested, the background color for the Status box on the strip and the background color for the Transcript row for the Intervention-Requested status are supposed to be set. See the design mockup.
2. When a job status is COmpleted-Error, the same bug is present for the transcript row, the red background color is missing
3. The title of the page for a Training Run is currently displaying the goals value, which is far too large for the space. Since we do not have a job name yet, simply output "Training Run" for this value instead
4. When pressing the "Start Job" on an invoice on the registered applications page, the modal dialog is displayed and for a period of 1-3s the message "No published recipes..." is displayed. I suspect the network call is delayed and this is a null check that instead should be displaying a loading message until the network call has completed, before it shows either the recipe or the "no recipe" message
5. Training runs are supposed to show an image icon in their transcript lines when a step has an artifact associatd with it. However, when I look at the transcript I do not see a step id on the transcript lines to associate with the artifacts in the artifacts list (which do have the step id). We need to make sure we include the step id on the transcipt lines, we need to make sure the image icon shows up on the transcript row, and we need to add the modal view when you click on an icon to show the screenshot for troubleshooting purposes. The dialog should show the screenshot, the transcript text across the top above it, and a close button to close the overlay. This should be as big as possible, consider leaving only 20px or similar around the edges and use the rest of the space to show the transcript description and the image.
6. The final screenshot that is captured and displayed on the job page when a job fails is too small to be useful. We should add a button to display a larger version and use the same dialog overlay from the prior item to open and display the screenshot larger.
7. When running a training run for real, the LLM chose a step id that already existed. We should identify this in our validation logic and ask it to try again. I have updated the prompt myself to try and work around this for now.
8. When a training job fails, we have no easy way to try it again. Add a "Retry Job" button in the top right of the job screen next to/after the "Export JSON" button. When clicked, this button will open the modal dialog from the new training button on the registered applications page with the values already filled out to match the job on the screen. The user can edit the values (or not) and then start the job, which wil create a new job like the Registered Applications page already does and redirect to the new job page.


